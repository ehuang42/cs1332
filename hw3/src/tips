removing from a BST

public method
    create dummy node
    root = recursive helper on root
    return dummy.data

helper (node, data, dummy)
    if (node == null) { data not found}
    if (data.equals(node.data) >>>> set dummy data
        no children > return null
        one chil > return child
        two >
            create temp dummy node
            node.right = succ(node.right, temp)
            node.data = temp.data
    if data < node.data >>>>>>>> node.left = recur left
    if data ....opposite
     return node

succH (node, temp)
    if node.left == null
        temp.data = node.data
        return node.right
    else {
        node.left = recur left }

    return node